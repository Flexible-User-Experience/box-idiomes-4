{% if not object.student.isPaymentExempt %}
    <a class="btn btn-sm btn-default" href="{{ admin.generateObjectUrl('pdf', object) }}" title="{{ 'backend.admin.invoice.download' | trans }}">
        <i class="far fa-file-pdf"></i>
    </a>
{% endif %}
